import mongoose from "mongoose"

export const createuserapiController = async (integrationData,getintegrationcommandqData) => {
    try {
    const { jsonpara: empdata } = integrationData;
    const { API: api, method } = getintegrationcommandqData;
    
    
    return userapiData
    }
    catch {
    }
}